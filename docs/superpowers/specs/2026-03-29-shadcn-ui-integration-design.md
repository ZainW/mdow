# shadcn/ui Integration for mdview-electron

**Date:** 2026-03-29
**Preset:** `base-nova` (headless base, Nova styling)
**Status:** Approved

## Overview

Initialize shadcn/ui with `base-nova` preset on the Electron renderer (Vite + React + TypeScript). Replace all interactive UI components with shadcn equivalents. Keep markdown body CSS as a scoped stylesheet re-mapped to shadcn's semantic color tokens.

## Component Mapping

| Current Component | shadcn Replacement | Notes |
|---|---|---|
| `CommandPalette` (custom overlay + input + result list + keyboard nav) | `Command` inside `Dialog` | Replaces all custom overlay, focus, keyboard, and search UI |
| `Sidebar` (custom div + resize handle + header/footer) | shadcn `Sidebar` (`SidebarProvider`, `Sidebar`, `SidebarHeader`, `SidebarContent`, `SidebarFooter`) | Custom resize drag handler wraps shadcn's `--sidebar-width` variable |
| `RecentsList` (section label + div list with active state) | `SidebarGroup` + `SidebarGroupLabel` + `SidebarMenu` + `SidebarMenuItem` + `SidebarMenuButton` | Active state via `isActive` prop or `data-active` |
| `FolderTree` (recursive divs with expand/collapse) | `Collapsible` + `SidebarGroup` + `SidebarMenuItem` tree | `CollapsibleTrigger`/`CollapsibleContent` for directories |
| `WelcomeView` (custom drop zone + open buttons) | `Card` (`CardHeader`/`CardContent`) for drop zone + `Button` components | Keep drag-and-drop handlers as-is |
| All `<button>` elements | `Button` with appropriate variants (`outline`, `default`) | Sidebar footer uses `variant="outline" size="sm"` |
| `.sidebar-divider` | `Separator` | Direct replacement |
| `.sidebar-section-label` | `SidebarGroupLabel` | Direct replacement |
| Quick Open trigger in sidebar header | `Button` with `variant="outline"` styled as search trigger | Keep Cmd+K shortcut display |

## shadcn Components to Install

```
sidebar button command dialog card separator collapsible scroll-area
```

## Theming

### Color Token Migration

| Old Variable | New shadcn Token |
|---|---|
| `--bg` | `--background` |
| `--text` | `--foreground` |
| `--text-secondary` | `--muted-foreground` |
| `--border` | `--border` |
| `--code-bg` | `--muted` |
| `--blockquote-border` | `--border` |
| `--blockquote-bg` | `--muted` |
| `--table-border` | `--border` |
| `--table-stripe` | `--muted` |
| `--link` | `--primary` |
| `--sidebar-bg` | `--sidebar` |
| `--sidebar-hover` | `--sidebar-accent` |
| `--sidebar-active` | `--sidebar-primary` |
| `--accent` | `--primary` |

### Dark Mode

- shadcn uses `.dark` class on `<html>` — same pattern as current (`html.dark`)
- The existing `useTheme` hook toggles the class; just ensure it sets `dark` (not a custom class)
- shadcn's CSS handles all token swaps via `.dark` selector automatically

## Sidebar Resize

shadcn's `Sidebar` component uses a `--sidebar-width` CSS variable but does not provide free-drag resizing. The approach:

1. Wrap the shadcn `Sidebar` in a container that handles the mousedown/mousemove resize logic (carried over from current implementation)
2. On drag, update both the zustand store (`sidebarWidth`) and the `--sidebar-width` CSS variable
3. The shadcn `Sidebar` respects this variable for its width
4. Min/max constraints remain 200px–400px

## Markdown Body CSS

Keep as a scoped `.markdown-body` stylesheet. Changes:

- Replace all `var(--old-token)` references with shadcn equivalents per the token mapping above
- Keep font-face declarations for Inter and Geist Mono
- Keep Shiki code highlighting dark mode overrides
- Keep mermaid diagram styles
- This CSS stays in a dedicated file (e.g., `markdown.css`) imported by `MarkdownView`

## File Structure Changes

```
src/renderer/src/
  components/
    ui/           # shadcn components (auto-generated)
  lib/
    utils.ts      # cn() helper (auto-generated by shadcn init)
  assets/
    styles/
      index.css   # shadcn's Tailwind CSS + theme tokens (replaces current)
      markdown.css # extracted markdown body styles (re-mapped tokens)
```

## What Stays the Same

- All zustand store logic (`useAppStore`, slices)
- All hooks (`useTheme`, `useFolderTree`, `useRecents`)
- All IPC/API calls (`window.api.*`)
- `MarkdownView` component structure (wrapper re-classed)
- Drag-and-drop logic in `App` and `WelcomeView`
- React Query usage
- `fuzzySearch` and `path-utils` libraries
- electron-vite config (add Tailwind v4 plugin)

## What Gets Deleted

- All custom CSS classes in `index.css`: `.sidebar`, `.sidebar-header`, `.sidebar-item`, `.quick-open-trigger`, `.welcome-view`, `.command-palette-*`, `.resize-handle`, `.content-area`, `.sidebar-footer`, `.sidebar-list`, `.sidebar-section-label`, `.sidebar-divider`
- Hand-rolled command palette overlay, keyboard navigation, and focus management (replaced by `Command` + `Dialog`)
- Manual emoji icon (`magnifying glass`) in sidebar header (replaced by a proper icon from lucide-react)
